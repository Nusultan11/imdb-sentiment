seed: 42

paths:
  raw_data: data/raw/imdb.csv
  model_output: artifacts/models/baseline.joblib
  metrics_output: artifacts/reports/metrics.json

data:
  text_column: review
  target_column: sentiment
  test_size: 0.2

model:
  type: logistic_regression
  max_features: 20000
  ngram_range: [1, 2]
  max_iter: 1000
